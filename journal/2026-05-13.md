# 2026-05-13 — YubiKey + git signing + dotfiles handoff

## Context

Session ran long; capturing state before context window runs out. The bulk of today: YubiKey provisioning + SSH signing + ADR authoring + dotfiles preparation. Resume from this entry next session.

## What's DONE on this Mac

### YubiKey + SSH + git signing (Phase 1, all working)

- **YubiKey** (2026 5C Nano, firmware 5.7+): FIDO2 PIN set, PIV PIN/PUK set, PIV management key = AES-192 with "Protect with PIN" (per Yubico SDK recommendation; PIV applet is unused so cascade risk has zero blast radius).
- **SSH key**: `~/.ssh/yubikey_5c_nano_2026{,.pub}` — FIDO2 resident ed25519-sk, `application=ssh:N4M3Z` (verified by decoding the .pub).
- **GitHub registrations**: `Nexus (a)` (authentication) + `Nexus (s)` (signing) — GPG-style suffix convention per PROV-0003.
- **brew openssh** + **libfido2** installed (Apple's `/usr/bin/ssh-keygen` lacks FIDO middleware; scripts call `/opt/homebrew/bin/ssh-keygen` explicitly).
- **theseal/ssh-askpass** LaunchAgent: sets `SSH_ASKPASS=/opt/homebrew/opt/ssh-askpass/bin/ssh-askpass` + `SUDO_ASKPASS` + placeholder `DISPLAY` for GUI sessions. `~/.zshenv` adds `export SSH_ASKPASS_REQUIRE=force`.
- **Git config**: `user.name=N4M3Z`, `user.email=martin.zeman@pm.me`, `gpg.format=ssh`, `gpg.ssh.program=/opt/homebrew/bin/ssh-keygen`, `user.signingkey=~/.ssh/yubikey_5c_nano_2026.pub`, `commit.gpgsign=true`, `tag.gpgsign=true`, `gpg.ssh.allowedSignersFile=~/.config/git/allowed_signers`.
- **Verified**: signed test commit landed (Mac-native dialog → FIDO2 PIN → YubiKey touch → `Good "git" signature`).

### Other landed today

- **gitleaks** installed (8.30.1) for pattern-based secret scanning
- **yubico-authenticator** (cask) installed; manages FIDO2 / PIV / OATH / OpenPGP PINs
- **glab**, **github** (Desktop), **raycast** added to Brewfile + installed
- **Claude Code experimental agent teams** enabled — `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` in `~/.claude/settings.json` (and in `manifests/claude-settings.json`); takes effect on Claude Code restart
- **dotfiles + dotfiles-private** rsynced from old Mac to `~/Downloads/` (NOT to `~/Developer/N4M3Z/`) — user did this manually. Next session: forensic-scan and decide what carries over.

## What's UNCOMMITTED in forge-provision

Many files. Status before session end:

- `manifests/Brewfile` — many additions (gh, jq, rtk, glab, yubico-authenticator, github, openssh, ssh-askpass tap+formula, gitleaks, raycast). Some entries committed in earlier commits this session; tail of additions is new.
- `manifests/claude-settings.json` — added `env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`
- `scripts/install/ssh-yubikey-key.sh` — uses `SSH_KEY_NAME` env var, brew's ssh-keygen, libfido2 path
- `scripts/configure/git-identity.sh` — reads `GIT_NAME` / `GIT_EMAIL` from .env
- `scripts/configure/git-signing-ssh.sh` — `gpg.format=ssh`, `gpg.ssh.program`, allowed_signers
- `scripts/lib/env.sh` — shell-agnostic path resolution (works in bash AND zsh; was bash-only)
- `.env` + `.env.example` — added `GIT_NAME`, `GIT_EMAIL`, `SSH_YUBIKEY_APPLICATION`, `SSH_KEY_NAME`, `TERMINAL_THEME`
- `docs/decisions/` — 8 ADRs all uncommitted:
  - ARCH-0001 Setup as provisioning artifacts
  - ARCH-0002 New machine provisioning order
  - ARCH-0003 Provisioning script naming
  - ARCH-0004 Use Forge AI
  - ARCH-0005 Dotfiles engine chezmoi
  - ARCH-0006 Commit signing
  - PROV-0001 Use Rsync
  - PROV-0002 macOS Terminal profile
  - PROV-0003 YubiKey

**Next session: commit these.** The user's Terminal has `SSH_ASKPASS_REQUIRE=force` exported so signed commits work; Claude Bash needs `SSH_ASKPASS=/opt/homebrew/opt/ssh-askpass/bin/ssh-askpass SSH_ASKPASS_REQUIRE=force` inline OR have the user commit.

## Decisions converged this session

| Topic | Decision |
| --- | --- |
| Shell prompt | starship (Rust, mature, the only mature Rust prompt; user accepted) |
| Zsh framework | Keep Prezto — still maintained, user comfortable, no migration tax. Starship overrides Prezto's prompt module (acceptable redundancy) |
| Shell history search | atuin, default-local (SQLite at `~/.local/share/atuin/`); `search_mode_shell_up_key_binding="fulltext"` replaces zsh-history-substring-search entirely. atuin's Up arrow does the substring-on-Up behavior the user relies on. |
| Dotfiles engine | chezmoi (ARCH-0005); migrate from stow; drop xero parent fork |
| Dotfiles repo name | Reuse `N4M3Z/dotfiles`; old xero fork → `dotfiles-legacy` (rename on GitHub). New repo is chezmoi-managed. |
| JS runtime | bun-first; install via brew, use `bun install -g` for global packages (ccline) |
| Claude Code statusline | `@cometix/ccline` (Rust binary, npm/bun-distributed), config at `~/.claude/ccline/config.toml`, registered via `settings.json` `statusLine.command` |
| dotfiles-private retention | Clone first, then decide per-file (per chezmoi `pass` template integration in ARCH-0005) |
| Secret scanning | gitleaks (deterministic, no LLM); optional deeper scan via LM Studio local model (`nvidia/nemotron-3-nano-omni`, `LMSTUDIO_API_KEY` in `~/.env`) |
| Commit signing | SSH default; OpenPGP opt-in per-repo (ARCH-0006) |
| GPG / ProtonMail key on YubiKey | Status `proposed` (was the "ARCH-0006 GPG and Proton key" draft — rolled into ARCH-0006 Commit signing's "OpenPGP opt-in" path). Setup deferred. |

## What's NOT done yet

- **Commit forge-provision changes** — many uncommitted (see list above). First signed commit on this Mac would dogfood the setup.
- **`~/.ssh/config`** — user rejected the scaffolded version; wants to base it on old machine's config + Include hosts + Include vendor/* patterns. Old config IS in the rsynced dotfiles repo (`dotfiles/ssh/.ssh/config`); copy + adapt.
- **dotfiles-private clone via SSH** — failed earlier (`Host key verification failed` then `ssh_askpass` not found). known_hosts now has github.com entry but ssh config needs `IdentityFile ~/.ssh/yubikey_5c_nano_2026` for github.com host (not yet written; this Mac's `~/.ssh/config` still doesn't exist). User chose to rsync from old Mac instead.
- **Forensic scan** of `~/Downloads/dotfiles{,-private}/` — gitleaks + optional LM Studio nemotron-3-nano-omni local LLM. Be careful with output redaction.
- **dotfiles → chezmoi migration** — multi-stage port. Sources rsynced; chezmoi not yet installed.
- **Tools not yet installed**: chezmoi, starship, atuin, bun. Add to Brewfile and brew-bundle.
- **`@cometix/ccline`** — install via bun once bun is in.
- **forge-core `ArchitectureDecision` skill update** — user wants it focused on "meaningful decisions" over chronology. Forge-core change; needs edit + `forge install` redeploy.
- **forge-core `NamingConventions` rule** — add an OpenSSH exception clause (per PROV-0003 note about snake_case for SSH key files).
- **Push themes + forge-provision to GitHub** — repos exist locally with commits; no GitHub remote yet.
- **macOS defaults script** — `scripts/configure/macos-defaults.sh` ran earlier with the starter set (Finder, Dock, Trackpad). User's actual flipped settings still pending enumeration.
- **App Store inventory** via `mas list` — never installed `mas`.

## Important cautions (next-session)

- **dotfiles-private contains sensitive material**: `gnupg/B83379DB.asc` (likely private OpenPGP export), `kube/.kube/` (bearer tokens), `password-store/.password-store/` (gpg-encrypted), `git/.gitconfig_local` (likely tokens/signingkey), `ssh/hosts` + `ssh/vendor/*` (host IPs and hostnames). **Never print contents to chat.** When inspecting, list filenames only; gitleaks runs with `--redact`.
- **Don't leak secret IPs/hostnames** in any tool output. The `ssh/hosts` and `ssh/vendor/*` files have these. User explicitly warned.
- **Don't use bash heredoc** in Bash tool calls (existing feedback memory).
- **Don't use `echo "===..."` separators** in Bash tool output (existing feedback memory).

## How to resume

1. Commit the uncommitted forge-provision work (ADRs, scripts, .env, Brewfile, env.sh). Signed commit via user's Terminal (has `SSH_ASKPASS_REQUIRE=force`) or inline env vars in Claude Bash.
2. Run `gitleaks detect --no-banner --redact -s ~/Downloads/dotfiles-private` and same on `~/Downloads/dotfiles`. Report counts, not raw matches.
3. Move `~/Downloads/dotfiles{,-private}/` to `~/Developer/N4M3Z/` (decide naming: replace the rsynced-from-Volumes version? Already at `dotfiles.gh-clone` for backup).
4. Inventory dotfiles-private structure (file listing only) — decide what becomes chezmoi-templated, what stays in a private repo (renamed), what gets re-keyed against the new YubiKey-managed setup.
5. Install missing tools (chezmoi, starship, atuin, bun, mas) — add to Brewfile + brew-bundle.
6. Start the chezmoi dotfiles migration in stages per ARCH-0005.
