# 2026-05-10 — Day Zero on the new Mac

## Context

Migrated from old Mac (mounted read-only at `/Volumes/N4M3Z`) to new Mac (`/Users/N4M3Z`). Goal: cold-boot agentic-first setup driven by `forge-provision` scripts.

Conventions inherited from existing repos rather than invented:
- Shell-script style: `check-mac` (no `set -euo pipefail`, severity codes, flat-in-verb dirs)
- Skill/agent/rule format: `forge-core`
- Book structure (when added later): `mac-setup`

Filename convention: **verb at directory, noun at filename** — `scripts/install/brew.sh`, `scripts/clone/references.sh`, `scripts/migrate/claude-history.sh`.

## Actions taken

1. Scaffolded `~/Developer/N4M3Z/forge-provision/`:
   - `README.md`, `.gitignore`, `CLAUDE.md`
   - `.env.example` (anonymized template) + `.env` (this machine's actual values, gitignored)
   - `provision.sh` (orchestrator stub, mirrors `check-mac/check.sh`)
   - `scripts/lib/env.sh` — sources `.env` (or `.env.example`) and exports all variables
   - `scripts/install/xcode-cli.sh` — `xcode-select --install` (idempotent — required before brew/git on a fresh Mac)
   - `scripts/install/claude-code.sh` — `curl -fsSL https://claude.ai/install.sh | bash` (idempotent)
   - `scripts/install/brew.sh` — `/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"` (idempotent)
   - `scripts/install/brew-bundle.sh` — `brew bundle install --file=manifests/Brewfile` (idempotent)
   - `scripts/install/terminal-theme.sh` — clones mbadolato/iTerm2-Color-Schemes, imports `${TERMINAL_THEME}.terminal`, sets it as Terminal.app default (idempotent)
   - `scripts/clone/references.sh` — clones reference repos into `${DEV_DIR}`
   - `manifests/Brewfile` — records the Proton suite + lm-studio (see "manual brew installs" below)
   - This journal entry

## State of this Mac (probed 2026-05-10)

- `claude`: installed at `~/.local/bin/claude`
- `brew`: installed at `/opt/homebrew/bin/brew`
- `git`: `/usr/bin/git` (from Xcode CLT)
- Xcode CLT: present at `/Library/Developer/CommandLineTools`

Running the install scripts on this machine will be no-ops (idempotency confirmed).

## Manual brew installs (captured 2026-05-10 / 2026-05-11)

The user installed these via `brew install` before we scripted things. Captured into `manifests/Brewfile`; `scripts/install/brew-bundle.sh` will be a no-op when run now (apps already present), but reproduces them on the next Mac.

```
brew install proton-drive proton-mail proton-mail-bridge proton-meet proton-pass protonvpn
brew install lm-studio
```

Note: the user may also have App Store installs of some Proton apps — running `brew bundle` after that creates a second copy. Review duplicates manually.

## Terminal theme

- Decided: NOT gogh. Gogh has no Homebrew formula and doesn't support Terminal.app on macOS (iTerm only).
- Chosen: `mbadolato/iTerm2-Color-Schemes` (26.9k ⭐, last updated 2026-05-10) — has a `terminal/` subdirectory of `.terminal` files Terminal.app imports natively. Same repo also covers `ghostty/`, `alacritty/`, `kitty/`, `wezterm/` — so when ghostty lands later, we add a per-emulator companion script that reads the same `TERMINAL_THEME`.
- Theme: **Dracula** (set via `TERMINAL_THEME` in `.env`)
- For warp + wave (also planned later): mbadolato has no `warp/` or `wave/` subdir; will use `catppuccin/warp` (323 ⭐) and similar separate sources, or just retype the palette per-emulator.

## Pending (in execution order)

1. Run `scripts/install/xcode-cli.sh` — expected: skip (CLT already present).
2. Run `scripts/install/claude-code.sh` — expected: skip.
3. Run `scripts/install/brew.sh` — expected: skip.
4. Run `scripts/install/brew-bundle.sh` — expected: all entries already installed, no-op.
5. Run `scripts/install/terminal-theme.sh` — clones mbadolato, imports Dracula, sets default.
6. Run `scripts/clone/references.sh` — clones forge-cli, check-mac, mac-setup, dotfiles, drduh's guide. `dotfiles-private` will fail until SSH key is set up on this Mac.
7. Copy `lib/style.sh`, `lib/helpers.sh`, and `LICENSE` (EUPL-1.2) from `check-mac/` into `forge-provision/`.
8. Author `scripts/configure/macos-defaults.sh` for the 3–4 settings the user flipped manually (list still pending from user).
9. Initial git commit.
10. Decide remaining `.claude` inclusions (`sessions/`, `tasks/`, `teams/`, `plans/`, `backups/` — parked).
11. Author `scripts/migrate/claude-history.sh` — encapsulates the rsync from `${OLD_CLAUDE_DIR}` of `projects/` + `history.jsonl`.

## Decisions reflected here

- **Cold-boot `.claude/`** rather than bulk-copying. Only `projects/` and `history.jsonl` from the old machine will be migrated. Agents, skills, rules rebuild from forge-* deploy.
- **Dotfiles engine: chezmoi** (deferred — after `forge-provision` is shaped).
- **Secrets: Proton Pass CLI** (`brew install protonpass/tap/pass-cli`) with chezmoi `protonPass` template function (v2.69.0+).
- **License: EUPL-1.2** (matches forge-core / check-mac).
- **`~/.claude.json`** at home root: do NOT overwrite. Old=143 KB contains plaintext tokens; new=23 KB stays as-is.
- **Adopt-into-forge** rather than plugin-install for "stupidly simple prompt" plugins (`superpowers`, `frontend-design`).
- **Configuration via `.env`** — every path/identity value lives in `.env` (gitignored) or `.env.example` (anonymized, committed). Scripts source `scripts/lib/env.sh`.
- **Filename convention: verb dir / noun file** — `scripts/install/brew.sh`, `scripts/clone/references.sh`, etc.
