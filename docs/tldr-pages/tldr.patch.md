- Show tealdeer's cache, configuration, and custom-page paths:

`tldr --show-paths`

- List all upstream and local pages:

`tldr --list`

- Access the same page through the personal dispatcher:

`sd tldr {{command}}`

- Validate the forge-provision page source without network access:

`./scripts/validate/tldr-pages.sh`
