# sabiql

> Browse, query, and edit PostgreSQL databases in a terminal interface.
> The first run requests a connection and stores its local configuration.
> More information: <https://github.com/riii111/sabiql>.

- Open the PostgreSQL browser:

`sabiql`

- Show the installed version:

`sabiql --version`
