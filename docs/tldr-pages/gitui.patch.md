- Open the repository browser with the local unified-diff configuration:

`gitui`

- Open GitUI in a specific repository:

`gitui --directory {{path/to/repository}}`

- Open the local configuration:

`${EDITOR:-zed} ~/.config/gitui/key_bindings.ron`
