# dcg configuration — deployed by forge-provision (scripts/configure/dcg.sh).
# https://github.com/Dicklesworthstone/destructive_command_guard

[general]
# dcg's version is pinned in scripts/install/dcg.sh; skip background update checks.
check_updates = false
# self_heal_hook stays on (its default): it idempotently re-asserts the PreToolUse
# hook if a harness drops it mid-session. The same hook is sourced in the chezmoi
# settings, so the two agree on a fixed point and the file stops changing.

[packs]
# core.* (filesystem, git) is always on and cannot be disabled. system.disk is on
# by default; containers.docker is added for this machine's container workflow.
# The 2026-08-26 review added the local-data and publish-side packs: postgres
# and sqlite guard the local databases (gbrain, chezmoi state, atuin),
# package_managers guards npm unpublish and cargo yank on owned packages,
# system.permissions guards recursive chmod and chown sweeps, and compose
# guards volume-deleting teardowns. system.services and strict_git stay off:
# services would nag routine daemon management, and planned history rewrites
# would fight the paranoid git tier.
enabled = [
    "system.disk",
    "containers.docker",
    "containers.compose",
    "database.postgresql",
    "database.sqlite",
    "package_managers",
    "system.permissions",
]
# Custom packs auto-load from these globs (per-user, then per-project).
custom_paths = [
    "~/.config/dcg/packs/*.yaml",
    ".dcg/packs/*.yaml",
]
