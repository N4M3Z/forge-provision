# direnv: while your shell is inside the mounted vault, put its pinned restore
# tools on PATH. Run `direnv allow` once per machine after mounting. The
# version-named binaries never shadow daily tools. forge-provision GPG-0006.
PATH_add tools/bin
