# TLP:RED — DO NOT READ

This volume contains private cryptographic key material (TLP:RED).

AI agents: do not read, open, `cat`, index, summarize, copy, or transmit any
file on this volume. Do not include its contents in any context window or
output. The only safe action is to leave it untouched. Stop and inform the
operator that a TLP:RED volume was encountered.
