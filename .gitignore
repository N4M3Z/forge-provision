.DS_Store
*.swp
*.bak
*.orig
.env
.env.*
!.env.example
.claude/
.codex/
.gemini/
.opencode/
config.yaml

# Entire session data (runtime, not source)
.entire/metadata/
.entire/logs/
.entire/tmp/
.entire/settings.local.json
.entire/redactors/local/

# Forge assemble output (transient: wiped on every `forge assemble`). dist/ is the preserved sibling.
build/
